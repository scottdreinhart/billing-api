/**
 * Central type definitions — pure domain types, no framework dependencies.
 * subscriptions, entitlements, payment intents, pricing tiers, purchase receipts, and webhook events
 */

// Add domain-specific types here
export {}
